---
"@modyra/widgets": minor
---

`mdy-dynamic-form` is declared among the shared classes

It belongs there by the rule — two themes select on it and it is no kind's anatomy — and was held
out only because the tree-shaking check could not tell a contract table that names it from a
component that shipped. That check now reads the position a bundler writes a selector into, so the
distinction it needed exists and the class is declared.
