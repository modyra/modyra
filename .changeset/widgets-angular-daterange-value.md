---
"@modyra/widgets": minor
"@modyra/angular": patch
---

Move date-range normalization, bounds, filter and endpoint-order transitions into Widgets and route Angular dirty/touched state through the shared bridge.
