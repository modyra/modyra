{
  "note": "Each entry names a package it must not. Rewrite the sentence to describe the behaviour; the list may only get shorter.",
  "references": [
    "packages/lit/src/adapter.ts:23: names @modyra/react — * `@modyra/react`'s `reactReactivity()`: `createLitForm`/`MdyFormControl",
    "packages/plain/src/schema.ts:3: names @modyra/react — * Contract field list — the same logic packages/react/src/dynamic/",
    "packages/preact/src/index.ts:31: names @modyra/react — * `@modyra/react`'s `reactReactivity()`: `useMdyForm` already runs on th",
    "packages/preact/src/index.ts:4: names @modyra/react — * A thin variant of `@modyra/react`: Preact has no signal primitive eith",
    "packages/preact/src/index.ts:91: names @modyra/react — * `@modyra/react`'s equivalent for the cross-runtime bug this fixes.",
    "packages/preact/test/preact.test.mjs:62: names @modyra/angular — * other runtime (Vue/Solid/Angular, or — as here — a custom one).",
    "packages/react/src/dynamic/dynamic-form.ts:39: names @modyra/angular — /** Builds the (validator-free) schema for a flat field list — every fie",
    "packages/react/src/dynamic/dynamic-form.ts:51: names @modyra/angular — /** Applies each field's Contract validators (via the same `buildDynamic",
    "packages/react/src/dynamic/dynamic-form.ts:63: names @modyra/angular — * config shape `parseDynamicForm()` produces and Angular's",
    "packages/react/src/dynamic/dynamic-form.ts:7: names @modyra/angular — * \"Parity\" with Angular's `<mdy-dynamic-form>` means the same value/",
    "packages/react/src/index.ts:94: names @modyra/angular — * Angular, vanilla), not just one created by this package's own",
    "packages/react/test/dynamic-form.test.mjs:3: names @modyra/angular — * Angular+React.\" buildDynamicFormSchema/applyDynamicValidators are the",
    "packages/react/test/dynamic-form.test.mjs:87: names @modyra/angular — test(\"select/multiselect get the automatic anti-tampering whitelist (one",
    "packages/react/test/headless-recipes.test.mjs:223: names @modyra/plain — // not yours to invent either. These are the same two functions `@modyra",
    "packages/react/test/headless-recipes.test.mjs:224: names @modyra/angular — // `@modyra/angular` call, so a two-column row is the same row whoever d",
    "packages/react/test/react.test.mjs:34: names @modyra/angular — * any other runtime (Vue/Solid/Angular, or — as here — a custom one).",
    "packages/svelte/src/index.ts:12: names @modyra/react — * runs on the core's `vanillaReactivity()` (same as `@modyra/react` and",
    "packages/svelte/src/index.ts:13: names @modyra/preact — * `@modyra/preact` — Svelte has no exported fine-grained signal any more",
    "packages/svelte/src/index.ts:38: names @modyra/react — * `@modyra/react`'s `reactReactivity()`: `createSvelteForm` already runs",
    "packages/vue/src/index.ts:196: names @modyra/angular — // unlike Angular's native computed(), which does. Declaring this",
    "packages/widgets/test/behavior.spec.mjs:27: names @modyra/angular — test(\"select keyboard policy resolves host actions without Angular\", () ",
    "packages/widgets/test/catalog-contract.spec.mjs:5: names @modyra/angular — test(\"the complete Angular catalog has ordered anatomy and closed part m"
  ]
}
