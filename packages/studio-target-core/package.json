{
  "name": "@modyra/studio-target-core",
  "version": "0.5.3",
  "private": true,
  "description": "Modyra Studio: the Core target scope (plan section 10) — generates a real @modyra/core createForm() definition, implementation stubs, and a submit example from a Studio project.",
  "license": "MIT",
  "type": "module",
  "main": "./dist/index.js",
  "types": "./dist/index.d.ts",
  "exports": {
    ".": {
      "types": "./dist/index.d.ts",
      "default": "./dist/index.js"
    }
  },
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "test": "npm run build && node --test test/*.test.mjs"
  },
  "sideEffects": false,
  "dependencies": {
    "@modyra/studio-codegen": "workspace:*",
    "@modyra/studio-contract": "workspace:*",
    "@modyra/studio-model": "workspace:*"
  }
}
