{
  "name": "@modyra/studio-preview",
  "version": "0.0.0",
  "private": true,
  "description": "Modyra Studio: live preview engine (plan section 11) — builds a real @modyra/core form instance directly from a Studio project (reads the model, never generated source) plus a configurable server mock for async validation/submit.",
  "license": "MIT",
  "type": "module",
  "main": "./dist/index.js",
  "types": "./dist/index.d.ts",
  "exports": {
    ".": {
      "types": "./dist/index.d.ts",
      "default": "./dist/index.js"
    }
  },
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "test": "npm run build && node --test test/*.test.mjs"
  },
  "sideEffects": false,
  "dependencies": {
    "@modyra/studio-model": "workspace:*",
    "@modyra/core": "workspace:*"
  }
}
