8085c5c4

The head a baseline recorder ran against, and whose output matched every committed linux image
here — 144 of 144 identical, nothing to commit. Run 33712882304.

This file exists because a page can change in a part the shots do not frame. The render is then
unchanged, the recorder produces the same bytes, and there is no image to land: without a record
that the run happened, the images stay forever older than the last change to the pages and the debt
can never be cleared.

Replace the line above only after a recorder run against a newer head has actually been compared.
It is evidence, not a formality — the same evidence a new image would have been.
